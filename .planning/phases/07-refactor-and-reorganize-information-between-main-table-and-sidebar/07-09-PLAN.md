---
phase: 07-refactor-and-reorganize-information-between-main-table-and-sidebar
plan: 09
type: execute
wave: 3
depends_on: ["07-07", "07-08"]
files_modified:
  - frontend/src/components/projects/tabs/CommitteeTab.tsx
  - frontend/src/components/projects/ProjectTabs.tsx
autonomous: true

must_haves:
  truths:
    - "CommitteeTab no longer shows Business Case section (moved to GeneralTab)"
    - "ProjectTabs tab order: General > Effort > Value > Change Impact > Committee > Budget > Actuals > History"
    - "People tab removed from ProjectTabs"
    - "Teams tab renamed to Effort in tab list"
  artifacts:
    - path: "frontend/src/components/projects/tabs/CommitteeTab.tsx"
      provides: "Committee tab with governance only"
      min_lines: 80
    - path: "frontend/src/components/projects/ProjectTabs.tsx"
      provides: "Updated tab configuration and order"
      min_lines: 60
  key_links:
    - from: "frontend/src/components/projects/ProjectTabs.tsx"
      to: "frontend/src/components/projects/tabs/GeneralTab.tsx"
      via: "import and TabsContent"
      pattern: "GeneralTab"
---

<objective>
Remove Business Case from CommitteeTab (moved to GeneralTab) and update ProjectTabs configuration for new tab order with People removed and Teams renamed to Effort.

Purpose: Complete the sidebar reorganization by updating tab navigation to match the new structure.

Output: Updated CommitteeTab and ProjectTabs components
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-refactor-and-reorganize-information-between-main-table-and-sidebar/07-CONTEXT.md
@.planning/phases/07-refactor-and-reorganize-information-between-main-table-and-sidebar/07-07-SUMMARY.md (if exists)
@frontend/src/components/projects/tabs/CommitteeTab.tsx
@frontend/src/components/projects/ProjectTabs.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Business Case section from CommitteeTab</name>
  <files>
    frontend/src/components/projects/tabs/CommitteeTab.tsx
  </files>
  <action>
    Update CommitteeTab to remove the Business Case file upload section. The tab should now only show:
    1. Committee Level indicator (Mandatory/Optional/Not Necessary)
    2. Workflow state machine with transitions
    3. Current state and action buttons

    Remove:
    - Business case file upload/download UI
    - Related state (uploading, file handlers)
    - File-related imports (Upload, Download, FileText icons if only used for business case)

    Keep:
    - Committee level determination
    - Workflow state machine visualization (draft → presented → discussion → approved/rejected)
    - State transition buttons

    The tab should focus purely on governance workflow. Example structure:
    ```typescript
    return (
      <div className="space-y-6">
        {/* Committee Level */}
        <div className="p-4 bg-muted/30 rounded-lg">
          <div className="flex items-center justify-between">
            <div>
              <h3 className="text-sm font-semibold text-muted-foreground uppercase tracking-wide">
                Committee Level
              </h3>
              <p className="text-xs text-muted-foreground mt-1">
                Based on project budget
              </p>
            </div>
            <Badge className={levelColor}>
              {levelText}
            </Badge>
          </div>
        </div>

        {/* Workflow State Machine */}
        <div className="border-t pt-6">
          <h3 className="text-sm font-semibold text-muted-foreground uppercase tracking-wide mb-4">
            Committee Workflow
          </h3>
          {/* ... existing workflow visualization and buttons ... */}
        </div>
      </div>
    );
    ```
  </action>
  <verify>
    `cat frontend/src/components/projects/tabs/CommitteeTab.tsx | grep -c "business-case"` returns 0 (no business case references)
  </verify>
  <done>
    CommitteeTab shows only governance workflow, Business Case removed
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ProjectTabs with new tab order and names</name>
  <files>
    frontend/src/components/projects/ProjectTabs.tsx
  </files>
  <action>
    Update ProjectTabs to:
    1. Remove People tab (merged into General)
    2. Rename "Teams" to "Effort"
    3. Set new tab order: General > Effort > Value > Change Impact > Committee > Budget > Actuals > History

    Update the tabs array:
    ```typescript
    const tabs = [
      { id: 'general', label: 'General' },
      { id: 'effort', label: 'Effort' },    // Renamed from 'teams'
      { id: 'value', label: 'Value' },
      { id: 'change-impact', label: 'Change Impact' },
      { id: 'committee', label: 'Committee' },
      { id: 'budget', label: 'Budget' },
      { id: 'actuals', label: 'Actuals' },
      { id: 'history', label: 'History' },
    ];
    ```

    Remove PeopleTab import and TabsContent for people:
    ```typescript
    // REMOVE this import:
    // import { PeopleTab } from './tabs/PeopleTab';

    // REMOVE this TabsContent:
    // <TabsContent value="people" className="mt-0 h-full">
    //   <PeopleTab ... />
    // </TabsContent>
    ```

    Update the teams TabsContent value to match:
    ```typescript
    <TabsContent value="effort" className="mt-0 h-full">
      <TeamsTab projectId={project.id} disabled={disabled} />
    </TabsContent>
    ```

    Ensure default tab remains "general":
    ```typescript
    <Tabs defaultValue="general" orientation="vertical" className="...">
    ```
  </action>
  <verify>
    `cat frontend/src/components/projects/ProjectTabs.tsx | grep "PeopleTab"` returns nothing (import removed)
    `cat frontend/src/components/projects/ProjectTabs.tsx | grep "'effort'"` shows the renamed tab
  </verify>
  <done>
    ProjectTabs has updated tab order, People removed, Teams renamed to Effort
  </done>
</task>

</tasks>

<verification>
- Run `npm run build` in frontend - no TypeScript errors
- ProjectTabs shows 8 tabs in correct order
- No "People" tab visible in sidebar
- "Teams" tab renamed to "Effort"
- CommitteeTab shows workflow only (no business case section)
- GeneralTab contains Business Case section
</verification>

<success_criteria>
- Tab order: General, Effort, Value, Change Impact, Committee, Budget, Actuals, History
- People tab removed from navigation
- Teams tab shows as "Effort" in navigation
- CommitteeTab contains only committee level and workflow state machine
- Business Case is accessible only through GeneralTab
- Default tab on open is General
</success_criteria>

<output>
After completion, create `.planning/phases/07-refactor-and-reorganize-information-between-main-table-and-sidebar/07-09-SUMMARY.md`
</output>
