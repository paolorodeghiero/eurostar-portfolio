---
phase: 07-refactor-and-reorganize-information-between-main-table-and-sidebar
plan: 08
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - frontend/src/components/projects/tabs/TeamsTab.tsx
  - frontend/src/components/projects/tabs/ValueTab.tsx
  - frontend/src/components/projects/tabs/BudgetTab.tsx
autonomous: true

must_haves:
  truths:
    - "TeamsTab renamed to Effort with global T-shirt badge at top"
    - "ValueTab shows large radar chart at top with dimension labels"
    - "BudgetTab shows OPEX and CAPEX as side-by-side cards at top"
  artifacts:
    - path: "frontend/src/components/projects/tabs/TeamsTab.tsx"
      provides: "Effort tab with global T-shirt display"
      min_lines: 80
    - path: "frontend/src/components/projects/tabs/ValueTab.tsx"
      provides: "Value tab with large radar chart"
      min_lines: 100
    - path: "frontend/src/components/projects/tabs/BudgetTab.tsx"
      provides: "Budget tab with OPEX/CAPEX cards"
      min_lines: 200
  key_links:
    - from: "frontend/src/components/projects/tabs/ValueTab.tsx"
      to: "recharts"
      via: "RadarChart import"
      pattern: "RadarChart.*PolarAngleAxis"
---

<objective>
Update sidebar tabs: add global effort T-shirt to Teams tab, add large radar chart to Value tab, and add OPEX/CAPEX side-by-side cards to Budget tab.

Purpose: Improve information density and visualization in sidebar tabs per user decisions.

Output: Updated TeamsTab, ValueTab, and BudgetTab components
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-refactor-and-reorganize-information-between-main-table-and-sidebar/07-CONTEXT.md
@.planning/phases/07-refactor-and-reorganize-information-between-main-table-and-sidebar/07-RESEARCH.md
@frontend/src/components/projects/tabs/TeamsTab.tsx
@frontend/src/components/projects/tabs/ValueTab.tsx
@frontend/src/components/projects/tabs/BudgetTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add global effort T-shirt to TeamsTab</name>
  <files>
    frontend/src/components/projects/tabs/TeamsTab.tsx
  </files>
  <action>
    Update TeamsTab to show global effort T-shirt badge at top, calculated from team sizes.

    Add imports:
    ```typescript
    import { deriveGlobalEffort, TSHIRT_COLORS } from '@/lib/effort-utils';
    ```

    Add global effort display at top of the component's return:
    ```typescript
    // Inside TeamsTab component, after loading state check:

    const globalEffort = deriveGlobalEffort(teams.map(t => ({ effortSize: t.effortSize })));

    return (
      <div className="space-y-6">
        {/* Global Effort Summary */}
        <div className="flex items-center justify-between p-4 bg-muted/30 rounded-lg">
          <div>
            <h3 className="text-sm font-semibold text-muted-foreground uppercase tracking-wide">
              Project Effort
            </h3>
            <p className="text-xs text-muted-foreground mt-1">
              Aggregate effort based on {teams.length} team{teams.length !== 1 ? 's' : ''}
            </p>
          </div>
          {globalEffort ? (
            <Badge className={`text-lg px-4 py-1 ${TSHIRT_COLORS[globalEffort] || 'bg-gray-300'}`}>
              {globalEffort}
            </Badge>
          ) : (
            <span className="text-muted-foreground">No teams assigned</span>
          )}
        </div>

        {/* Divider */}
        <div className="border-t" />

        {/* Existing teams list below */}
        {/* ... rest of existing TeamsTab content ... */}
      </div>
    );
    ```

    Note: The existing TeamsTab content (team chips, add team functionality) should remain below the global effort summary.
  </action>
  <verify>
    `cat frontend/src/components/projects/tabs/TeamsTab.tsx | grep "deriveGlobalEffort"` shows import is used
  </verify>
  <done>
    TeamsTab shows global effort T-shirt badge at top, followed by team list
  </done>
</task>

<task type="auto">
  <name>Task 2: Add large radar chart to ValueTab</name>
  <files>
    frontend/src/components/projects/tabs/ValueTab.tsx
  </files>
  <action>
    Update ValueTab to show a large radar chart at the top with dimension labels visible, then individual score cards below.

    Add imports:
    ```typescript
    import {
      Radar,
      RadarChart,
      PolarGrid,
      PolarAngleAxis,
      PolarRadiusAxis,
      ResponsiveContainer,
    } from 'recharts';
    ```

    Add radar chart component at top of the tab's return:
    ```typescript
    // Transform values for radar chart
    const chartData = values.map(v => ({
      dimension: v.outcomeName,
      score: v.score ?? 0,
      fullMark: 5,
    }));

    const averageScore = values.length > 0
      ? values.reduce((sum, v) => sum + (v.score ?? 0), 0) / values.length
      : 0;

    return (
      <div className="space-y-6">
        {/* Large Radar Chart */}
        {values.length > 0 && (
          <div className="p-4 bg-muted/30 rounded-lg">
            <div className="flex items-center justify-between mb-4">
              <h3 className="text-sm font-semibold text-muted-foreground uppercase tracking-wide">
                Value Score Overview
              </h3>
              <span className="text-lg font-semibold">
                Avg: {averageScore.toFixed(1)} / 5
              </span>
            </div>
            <div className="h-[250px]">
              <ResponsiveContainer width="100%" height="100%">
                <RadarChart data={chartData} margin={{ top: 20, right: 30, bottom: 20, left: 30 }}>
                  <PolarGrid stroke="#e5e7eb" />
                  <PolarAngleAxis
                    dataKey="dimension"
                    tick={{ fill: 'hsl(var(--muted-foreground))', fontSize: 12 }}
                  />
                  <PolarRadiusAxis
                    angle={90}
                    domain={[0, 5]}
                    tick={{ fill: 'hsl(var(--muted-foreground))', fontSize: 10 }}
                  />
                  <Radar
                    name="Score"
                    dataKey="score"
                    stroke="hsl(var(--primary))"
                    fill="hsl(var(--primary))"
                    fillOpacity={0.5}
                  />
                </RadarChart>
              </ResponsiveContainer>
            </div>
          </div>
        )}

        {/* Divider */}
        <div className="border-t" />

        {/* Existing score cards below */}
        <h3 className="text-sm font-semibold text-muted-foreground uppercase tracking-wide">
          Individual Scores
        </h3>
        {/* ... rest of existing ValueTab content (score cards) ... */}
      </div>
    );
    ```
  </action>
  <verify>
    `cat frontend/src/components/projects/tabs/ValueTab.tsx | grep "ResponsiveContainer"` shows recharts usage
  </verify>
  <done>
    ValueTab shows large radar chart at top with dimension labels, score cards below
  </done>
</task>

<task type="auto">
  <name>Task 3: Add OPEX/CAPEX side-by-side cards to BudgetTab</name>
  <files>
    frontend/src/components/projects/tabs/BudgetTab.tsx
  </files>
  <action>
    Update BudgetTab to show OPEX and CAPEX as two side-by-side cards at top with Edit button, replacing the hover-to-edit pattern.

    Replace the existing "Total Budget Header with Hover Edit" section with:
    ```typescript
    // State for edit mode
    const [editMode, setEditMode] = useState(false);

    // ... in render, replace HoverCard section with:

    {/* OPEX/CAPEX Cards */}
    <div className="grid grid-cols-2 gap-4 mb-6">
      {/* OPEX Card */}
      <div className="border rounded-lg p-4 space-y-2">
        <div className="flex items-center justify-between">
          <span className="text-sm text-muted-foreground">OPEX</span>
          <Badge variant="outline" className="text-xs">Operating</Badge>
        </div>
        {editMode ? (
          <Input
            type="text"
            value={localOpex}
            onChange={(e) => setLocalOpex(e.target.value)}
            placeholder="0.00"
            disabled={disabled || !currency}
            className="text-lg font-semibold"
          />
        ) : (
          <div className="text-2xl font-semibold">
            {formatCurrency(budget?.convertedOpex || budget?.opexBudget || '0', displayCurrency)}
          </div>
        )}
      </div>

      {/* CAPEX Card */}
      <div className="border rounded-lg p-4 space-y-2">
        <div className="flex items-center justify-between">
          <span className="text-sm text-muted-foreground">CAPEX</span>
          <Badge variant="outline" className="text-xs">Capital</Badge>
        </div>
        {editMode ? (
          <Input
            type="text"
            value={localCapex}
            onChange={(e) => setLocalCapex(e.target.value)}
            placeholder="0.00"
            disabled={disabled || !currency}
            className="text-lg font-semibold"
          />
        ) : (
          <div className="text-2xl font-semibold">
            {formatCurrency(budget?.convertedCapex || budget?.capexBudget || '0', displayCurrency)}
          </div>
        )}
      </div>
    </div>

    {/* Edit/Save Button and Total */}
    <div className="flex items-center justify-between mb-4">
      <div className="flex items-center gap-3">
        <span className="text-sm text-muted-foreground">Total:</span>
        <span className="text-lg font-semibold">
          {formatCurrency(budget?.totalBudget || '0', displayCurrency)}
        </span>
        {budget?.costTshirt && (
          <Badge className={TSHIRT_COLORS[budget.costTshirt] || 'bg-gray-300'}>
            {budget.costTshirt}
          </Badge>
        )}
      </div>
      <Button
        variant={editMode ? 'default' : 'outline'}
        size="sm"
        onClick={() => setEditMode(!editMode)}
        disabled={disabled || !currency}
      >
        {editMode ? 'Done' : 'Edit Budget'}
      </Button>
    </div>

    {statusText && editMode && (
      <span className="text-xs text-muted-foreground block mb-4">{statusText}</span>
    )}
    ```

    Remove the HoverCard import and related code. Keep the existing allocations section below.
  </action>
  <verify>
    `cat frontend/src/components/projects/tabs/BudgetTab.tsx | grep "grid-cols-2"` shows side-by-side layout
  </verify>
  <done>
    BudgetTab shows OPEX and CAPEX as side-by-side cards with Edit button
  </done>
</task>

</tasks>

<verification>
- Run `npm run build` in frontend - no TypeScript errors
- TeamsTab shows global effort badge at top
- ValueTab shows large radar chart with dimension labels
- BudgetTab shows OPEX/CAPEX as two cards in a row
- All existing functionality (add team, edit score, add allocation) still works
</verification>

<success_criteria>
- TeamsTab displays: Global effort T-shirt badge at top + team list below
- ValueTab displays: Large radar chart (250px height) with labels + score cards below
- BudgetTab displays: OPEX card + CAPEX card side-by-side + Edit button + allocations below
- Radar chart shows all 5 dimension labels around the perimeter
- OPEX/CAPEX cards show large amount text (text-2xl)
- Edit mode toggles between view and input fields
</success_criteria>

<output>
After completion, create `.planning/phases/07-refactor-and-reorganize-information-between-main-table-and-sidebar/07-08-SUMMARY.md`
</output>
