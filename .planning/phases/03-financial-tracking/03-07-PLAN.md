---
phase: 03-financial-tracking
plan: 07
type: execute
wave: 3
depends_on: ["03-03", "03-04"]
files_modified:
  - frontend/src/components/projects/ActualsSummary.tsx
  - frontend/src/components/projects/ProjectSidebar.tsx
  - frontend/src/components/ActualsUploadDialog.tsx
  - frontend/src/pages/PortfolioPage.tsx
  - frontend/src/lib/actuals-api.ts
autonomous: true

must_haves:
  truths:
    - "Project sidebar shows actuals summary with totals vs budget"
    - "User can upload actuals via Excel from portfolio page"
    - "Upload shows validation errors and success count"
    - "Invoices with failed extraction are flagged for attention"
  artifacts:
    - path: "frontend/src/components/projects/ActualsSummary.tsx"
      provides: "Actuals summary display"
      min_lines: 80
    - path: "frontend/src/components/ActualsUploadDialog.tsx"
      provides: "Excel upload dialog"
      min_lines: 100
    - path: "frontend/src/lib/actuals-api.ts"
      provides: "Actuals API client"
      exports: ["fetchProjectActuals", "uploadReceipts", "uploadInvoices"]
  key_links:
    - from: "ProjectSidebar.tsx"
      to: "ActualsSummary.tsx"
      via: "import"
      pattern: "import.*ActualsSummary"
    - from: "PortfolioPage.tsx"
      to: "ActualsUploadDialog.tsx"
      via: "import"
      pattern: "import.*ActualsUploadDialog"
---

<objective>
Add actuals summary to project sidebar and Excel upload dialog to portfolio page.

Purpose: ACTL-08 - Actuals summary in sidebar. ACTL-01, ACTL-02 - Excel import for receipts and invoices.
Output: Read-only actuals display and upload functionality.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@frontend/src/components/projects/ProjectSidebar.tsx
@frontend/src/pages/PortfolioPage.tsx
@frontend/src/lib/project-api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create actuals API client</name>
  <files>frontend/src/lib/actuals-api.ts</files>
  <action>
Create frontend/src/lib/actuals-api.ts:

Define interfaces:
```typescript
interface ProjectActualsSummary {
  totalReceipts: string;
  totalInvoices: string;
  totalActuals: string;
  currency: string;
  budgetTotal: string;
  budgetRemaining: string;
  percentUsed: string;
  invoicesNeedingAttention: number; // competenceMonthExtracted = false
}

interface ImportResult {
  imported: number;
  errors: Array<{ row?: number; index?: number; message: string }>;
  extractionWarnings?: number;
}
```

Export functions:
1. fetchProjectActualsSummary(projectId: number): Promise<ProjectActualsSummary>
   - GET /api/projects/:id/actuals/summary (need to add this endpoint in backend)

2. uploadReceiptsExcel(file: File): Promise<ImportResult>
   - POST /api/actuals/receipts/upload
   - Use FormData for file upload

3. uploadInvoicesExcel(file: File): Promise<ImportResult>
   - POST /api/actuals/invoices/upload
   - Use FormData for file upload

4. uploadReceiptsJson(data: Array<ReceiptInput>): Promise<ImportResult>
   - POST /api/actuals/receipts/import (JSON body)

5. uploadInvoicesJson(data: Array<InvoiceInput>): Promise<ImportResult>
   - POST /api/actuals/invoices/import (JSON body)

Note: Add Excel upload endpoints to backend if not already present (reuse excel-parser).
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>actuals-api.ts exports all actuals functions</done>
</task>

<task type="auto">
  <name>Task 2: Create actuals summary component and upload dialog</name>
  <files>
frontend/src/components/projects/ActualsSummary.tsx
frontend/src/components/ActualsUploadDialog.tsx
frontend/src/components/projects/ProjectSidebar.tsx
frontend/src/pages/PortfolioPage.tsx
  </files>
  <action>
**Create ActualsSummary.tsx:**
- Read-only component showing actuals for a project
- Layout:
  - Header: "Actuals" with currency badge
  - Rows:
    - Receipts Total: {amount}
    - Invoices Total: {amount}
    - **Total Actuals: {amount}**
    - Budget: {amount}
    - Remaining: {amount} (green if positive, red if negative)
  - Progress bar showing percentUsed
  - Alert if invoicesNeedingAttention > 0: "{N} invoices need competence month review"
- Props: projectId: number
- Fetches data on mount with useEffect

**Update ProjectSidebar.tsx:**
- Import ActualsSummary
- Add below ProjectTabs (before footer), inside a collapsible section
- Only show if project has budget set (budgetCurrency not null)

**Create ActualsUploadDialog.tsx:**
- Dialog with tabs for Receipts and Invoices
- Each tab has:
  - File input accepting .xlsx, .xls
  - "Upload" button
  - Results display:
    - Success: "Imported {N} {receipts|invoices}"
    - Errors: List with row numbers
    - For invoices: "Warning: {N} invoices need competence month review"
  - "Download Template" link (optional, can be static Excel template)

**Update PortfolioPage.tsx:**
- Add "Upload Actuals" button in top bar (next to alerts)
- Opens ActualsUploadDialog
- On successful upload, optionally refresh current project if sidebar open
  </action>
  <verify>
cd frontend && npm run build
Open project sidebar - actuals summary visible
Click Upload Actuals button - dialog opens
  </verify>
  <done>Actuals summary in sidebar, upload dialog accessible from portfolio page</done>
</task>

<task type="auto">
  <name>Task 3: Add actuals summary and Excel upload endpoints to backend</name>
  <files>
backend/src/routes/projects/projects.ts
backend/src/routes/actuals/receipts.ts
backend/src/routes/actuals/invoices.ts
  </files>
  <action>
**Add to projects.ts (or create project-actuals.ts):**
GET /api/projects/:projectId/actuals/summary
- Calculate totals from receipts and invoices tables
- Join with project for budget info
- Count invoices where competenceMonthExtracted = false
- Return ProjectActualsSummary shape

**Add to receipts.ts:**
POST /api/actuals/receipts/upload
- Register @fastify/multipart if not already
- Accept Excel file upload
- Parse with excel-parser (reuse from budget-lines)
- Validate rows with Zod schema:
  - ProjectId (string, PRJ-XXXX format)
  - ReceiptNumber (optional string)
  - Amount (number)
  - Currency (string, 3 chars)
  - Date (date string)
  - Description (optional string)
- Map ProjectId to internal id (lookup in projects table)
- Insert valid rows
- Return { imported, errors }

**Add to invoices.ts:**
POST /api/actuals/invoices/upload
- Same pattern as receipts
- Additional: run competence month extraction
- Schema includes InvoiceNumber (required), Description (for extraction)
- Return { imported, errors, extractionWarnings }
  </action>
  <verify>
cd backend && npx tsc --noEmit
Test endpoints with curl and sample Excel files
  </verify>
  <done>Backend endpoints for actuals summary and Excel upload</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Start both frontend and backend
2. Open project with budget set - actuals summary shows
3. Click "Upload Actuals" in portfolio - dialog opens
4. Upload receipts Excel - see success/error feedback
5. Upload invoices Excel - see extraction warning count
6. Verify sidebar updates after upload
</verification>

<success_criteria>
- Actuals summary shows in project sidebar with budget comparison
- Upload Actuals button accessible from portfolio page
- Excel upload works for both receipts and invoices
- Validation errors displayed clearly
- Extraction warning count shown for invoices
</success_criteria>

<output>
After completion, create `.planning/phases/03-financial-tracking/03-07-SUMMARY.md`
</output>
