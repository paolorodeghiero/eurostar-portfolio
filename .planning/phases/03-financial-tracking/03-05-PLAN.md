---
phase: 03-financial-tracking
plan: 05
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - frontend/src/pages/admin/BudgetLinesPage.tsx
  - frontend/src/pages/admin/AdminLayout.tsx
  - frontend/src/lib/budget-lines-api.ts
autonomous: true

must_haves:
  truths:
    - "Admin can see list of budget lines with allocated/available amounts"
    - "Admin can upload Excel file to import budget lines"
    - "Import shows validation errors for each row"
    - "Budget lines in use cannot be deleted"
  artifacts:
    - path: "frontend/src/pages/admin/BudgetLinesPage.tsx"
      provides: "Budget lines admin page"
      min_lines: 150
    - path: "frontend/src/lib/budget-lines-api.ts"
      provides: "Budget lines API client"
      exports: ["fetchBudgetLines", "importBudgetLines", "deleteBudgetLine"]
  key_links:
    - from: "BudgetLinesPage.tsx"
      to: "budget-lines-api.ts"
      via: "import"
      pattern: "import.*budget-lines-api"
    - from: "AdminLayout.tsx"
      to: "BudgetLinesPage"
      via: "route"
      pattern: "budget-lines"
---

<objective>
Create budget lines admin page with Excel import functionality.

Purpose: BLIN-03 - Budget line import available in Admin GUI.
Output: Admin page with DataTable displaying budget lines and Excel upload dialog.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@frontend/src/pages/admin/DepartmentsPage.tsx
@frontend/src/pages/admin/AdminLayout.tsx
@frontend/src/components/ui/data-table.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create budget lines API client</name>
  <files>frontend/src/lib/budget-lines-api.ts</files>
  <action>
Create frontend/src/lib/budget-lines-api.ts:

Define BudgetLine interface:
```typescript
interface BudgetLine {
  id: number;
  company: string;
  departmentId: number;
  departmentName: string;
  costCenterId: number;
  costCenterCode: string;
  lineValue: string;
  lineAmount: string;
  currency: string;
  type: 'CAPEX' | 'OPEX';
  fiscalYear: number;
  allocatedAmount: string;
  availableAmount: string;
}
```

Export functions:
1. fetchBudgetLines(filters?: { fiscalYear?, company?, type? }): Promise<BudgetLine[]>
2. importBudgetLines(file: File): Promise<{ imported: number, errors: Array<{ row: number, message: string }> }>
   - Use FormData to upload file
   - POST to /api/admin/budget-lines/import
3. deleteBudgetLine(id: number): Promise<void>
   - Handle 409 error (in use) with proper error message

Follow existing API client patterns from project-api.ts.
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>budget-lines-api.ts exports all CRUD functions</done>
</task>

<task type="auto">
  <name>Task 2: Create budget lines admin page</name>
  <files>
frontend/src/pages/admin/BudgetLinesPage.tsx
frontend/src/pages/admin/AdminLayout.tsx
  </files>
  <action>
Create frontend/src/pages/admin/BudgetLinesPage.tsx following DepartmentsPage pattern:

1. Use DataTable with columns:
   - Company (THIF/EIL)
   - Department (name)
   - Cost Center (code)
   - Line Value (description)
   - Amount (formatted with currency)
   - Type (CAPEX/OPEX badge)
   - Fiscal Year
   - Allocated (formatted)
   - Available (formatted, highlight red if 0 or negative)
   - Actions (delete button, disabled if allocated > 0)

2. Add filter controls at top:
   - Fiscal Year dropdown (current year default)
   - Company dropdown (All, THIF, EIL)
   - Type dropdown (All, CAPEX, OPEX)

3. Add "Import Excel" button that opens a Dialog:
   - File input accepting .xlsx, .xls
   - Upload button
   - Show progress/loading state
   - On success: show imported count, refresh table
   - On errors: show error list with row numbers

4. Delete confirmation dialog (same pattern as other admin pages)
   - Show budget line details
   - Disabled with message when allocatedAmount > 0

Update AdminLayout.tsx:
- Add "Budget Lines" to sidebar navigation
- Route: /admin/budget-lines
- Icon: DollarSign or similar from lucide-react

Format currency amounts with Intl.NumberFormat matching the currency code.
  </action>
  <verify>
cd frontend && npm run build
Navigate to /admin/budget-lines - page renders
  </verify>
  <done>Budget lines admin page with table, filters, import dialog, and delete protection</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. cd frontend && npm run dev
2. Navigate to /admin/budget-lines
3. Verify empty state shows correctly
4. Test import with sample Excel file
5. Verify filters work (fiscal year, company, type)
6. Verify delete disabled for items with allocations
</verification>

<success_criteria>
- Budget Lines page accessible from admin sidebar
- DataTable shows all budget line fields with proper formatting
- Import dialog accepts Excel and shows success/errors
- Delete button disabled with tooltip when item has allocations
- Filters update table content
</success_criteria>

<output>
After completion, create `.planning/phases/03-financial-tracking/03-05-SUMMARY.md`
</output>
