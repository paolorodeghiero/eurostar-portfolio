---
phase: 03-financial-tracking
plan: 06
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - frontend/src/components/projects/tabs/BudgetTab.tsx
  - frontend/src/components/projects/ProjectTabs.tsx
  - frontend/src/lib/project-budget-api.ts
autonomous: true

must_haves:
  truths:
    - "User can set OPEX and CAPEX budgets with currency selector"
    - "User can see derived cost T-shirt size"
    - "User can add/remove budget line allocations"
    - "Allocation amount is validated against available"
    - "Alert shows when allocations don't match declared totals"
  artifacts:
    - path: "frontend/src/components/projects/tabs/BudgetTab.tsx"
      provides: "Budget tab component"
      min_lines: 200
    - path: "frontend/src/lib/project-budget-api.ts"
      provides: "Project budget API client"
      exports: ["fetchProjectBudget", "updateProjectBudget", "addAllocation"]
  key_links:
    - from: "BudgetTab.tsx"
      to: "project-budget-api.ts"
      via: "import"
      pattern: "import.*project-budget-api"
    - from: "ProjectTabs.tsx"
      to: "BudgetTab"
      via: "import"
      pattern: "import.*BudgetTab"
---

<objective>
Create Budget tab in project sidebar for managing OPEX/CAPEX and budget line allocations.

Purpose: BUDG-01 through BUDG-05 - Complete budget management UI with validation alerts.
Output: Budget tab component with inputs, allocations table, and validation alerts.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@frontend/src/components/projects/tabs/TeamsTab.tsx
@frontend/src/components/projects/ProjectTabs.tsx
@frontend/src/lib/project-api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project budget API client</name>
  <files>frontend/src/lib/project-budget-api.ts</files>
  <action>
Create frontend/src/lib/project-budget-api.ts:

Define interfaces:
```typescript
interface ProjectBudget {
  opexBudget: string | null;
  capexBudget: string | null;
  budgetCurrency: string | null;
  costTshirt: string | null;
  totalBudget: string;
  totalAllocated: string;
  allocationMatch: boolean;
  allocations: BudgetAllocation[];
}

interface BudgetAllocation {
  budgetLineId: number;
  lineValue: string;
  company: string;
  lineAmount: string;
  allocationAmount: string;
  availableAmount: string;
  currency: string;
}

interface AvailableBudgetLine {
  id: number;
  lineValue: string;
  company: string;
  available: string;
  currency: string;
}
```

Export functions:
1. fetchProjectBudget(projectId: number): Promise<ProjectBudget>
2. updateProjectBudget(projectId: number, data: { opexBudget?, capexBudget?, budgetCurrency? }): Promise<ProjectBudget>
3. fetchAvailableBudgetLines(currency: string): Promise<AvailableBudgetLine[]>
   - GET /api/admin/budget-lines with currency filter, only those with available > 0
4. addBudgetAllocation(projectId: number, budgetLineId: number, amount: string): Promise<BudgetAllocation>
   - Handle 400 error (exceeds available) with user-friendly message
5. updateBudgetAllocation(projectId: number, budgetLineId: number, amount: string): Promise<BudgetAllocation>
6. removeBudgetAllocation(projectId: number, budgetLineId: number): Promise<void>
  </action>
  <verify>cd frontend && npx tsc --noEmit</verify>
  <done>project-budget-api.ts exports all budget management functions</done>
</task>

<task type="auto">
  <name>Task 2: Create Budget tab component</name>
  <files>
frontend/src/components/projects/tabs/BudgetTab.tsx
frontend/src/components/projects/ProjectTabs.tsx
  </files>
  <action>
Create frontend/src/components/projects/tabs/BudgetTab.tsx:

**Layout:**
1. Budget Totals section:
   - Currency selector (Select component from shadcn)
   - OPEX Budget input (numeric, formatted)
   - CAPEX Budget input (numeric, formatted)
   - Total row (calculated, read-only)
   - Cost T-shirt badge (auto-derived, read-only) - XS/S/M/L/XL/XXL with color coding

2. Allocation Match Alert (BUDG-04):
   - Show warning banner if allocationMatch is false
   - Text: "Allocated amount ({totalAllocated}) does not match declared budget ({totalBudget})"
   - Use Alert component with yellow/warning styling

3. Budget Allocations section:
   - Table showing current allocations:
     - Budget Line (lineValue + company)
     - Allocated amount (editable inline input)
     - Available in line
     - Remove button
   - "Add Allocation" button opens Command+Popover:
     - Search available budget lines (matching currency)
     - Show available amount for each
     - Amount input field
     - Add button (disabled if amount > available)

**Behavior:**
- Budget inputs auto-save using useAutoSave pattern (2500ms debounce)
- On budget save: costTshirt updates automatically
- Allocation changes: immediate save (no debounce) since they're discrete actions
- Show validation error inline when allocation exceeds available

**Props:**
- project: Project
- disabled: boolean (for read-only mode)

Update ProjectTabs.tsx:
- Add "Budget" tab between existing tabs
- Import and render BudgetTab
- Pass project and disabled props
  </action>
  <verify>
cd frontend && npm run build
Open project sidebar - Budget tab visible and functional
  </verify>
  <done>Budget tab with totals, auto-derived T-shirt, allocations table, and validation alerts</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. cd frontend && npm run dev
2. Open a project in sidebar
3. Navigate to Budget tab
4. Set OPEX/CAPEX values - verify costTshirt updates
5. Add budget allocation - verify validation
6. Verify warning shows when totals don't match allocations
</verification>

<success_criteria>
- Budget tab shows in project sidebar
- Currency selector, OPEX, CAPEX inputs work
- Cost T-shirt auto-derives and displays
- Allocations can be added/edited/removed
- Warning banner shows when totals mismatch
- Allocation blocked with error when exceeding available
</success_criteria>

<output>
After completion, create `.planning/phases/03-financial-tracking/03-06-SUMMARY.md`
</output>
