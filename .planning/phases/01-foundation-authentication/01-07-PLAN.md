---
phase: 01-foundation-authentication
plan: 07
type: execute
wave: 5
depends_on: ["01-06"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "User can log in via EntraID and session persists across browser refresh"
    - "Admin group members can access referential management while regular users cannot"
    - "API validates EntraID tokens on every request and rejects invalid tokens"
    - "Dev mode allows local development without authentication when enabled"
    - "All referentials can be created, edited, and deleted with usage protection"
    - "Each referential item shows usage count and deletion is blocked when item is in use"
  artifacts: []
  key_links: []
---

<objective>
Verify all Phase 1 success criteria are met through manual testing and visual inspection.

Purpose: Ensure the foundation and authentication system works end-to-end before proceeding.
Output: Verified phase completion ready for Phase 2.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-authentication/01-06-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 1 implementation:
    - Backend: Fastify 5 with PostgreSQL, EntraID JWT validation, dev mode bypass
    - Frontend: React 19 with MSAL authentication, session persistence
    - Database: All 9 referential tables with constraints
    - API: CRUD endpoints for all referentials with usage tracking
    - Admin GUI: Data tables with create/edit/delete, usage counts, delete protection
  </what-built>
  <how-to-verify>
    1. **Start services:**
       ```bash
       # Terminal 1: Database
       docker-compose up -d

       # Terminal 2: Backend (dev mode)
       cd backend && DEV_MODE=true npm run dev

       # Terminal 3: Frontend
       cd frontend && npm run dev
       ```

    2. **Verify authentication flow:**
       - Open http://localhost:5173
       - Should see login page (unauthenticated state)
       - Click "Sign in with Microsoft" (will show popup - may fail without real EntraID)
       - With dev mode: refresh page, should remain "authenticated" via dev user

    3. **Verify admin GUI:**
       - Navigate to http://localhost:5173/admin
       - Should see list of 9 referential types
       - Click "Departments" in sidebar

    4. **Verify CRUD operations:**
       - Click "Add New" - dialog opens
       - Enter "Information Systems" - click Save
       - Department appears in table with usage count = 0
       - Click Edit - change name - Save
       - Click Delete - should succeed (not in use)

    5. **Verify usage protection:**
       - Create new department "Test Dept"
       - Go to Teams, create team "Test Team" with department = "Test Dept"
       - Go back to Departments
       - "Test Dept" should show usage count = 1
       - Delete button should be disabled or show warning
       - Attempting delete should fail with message

    6. **Verify API directly:**
       ```bash
       # Health check
       curl http://localhost:3000/health

       # List referential types (dev mode = admin)
       curl http://localhost:3000/api/admin

       # CRUD operations
       curl -X POST http://localhost:3000/api/admin/statuses \
         -H "Content-Type: application/json" \
         -d '{"name":"Draft","color":"#808080","displayOrder":1}'
       ```

    7. **Verify session persistence:**
       - While logged in, refresh browser (F5)
       - Should still be authenticated (not redirected to login)

    8. **Verify all referential types work:**
       - Navigate through each referential type in admin sidebar
       - Each should load without errors
       - Create at least one item in each type
  </how-to-verify>
  <resume-signal>
    Type "phase 1 verified" if all checks pass.
    If issues found, describe the specific failures and Claude will address them.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 1 Success Criteria (from ROADMAP.md):
1. User can log in via EntraID and session persists across browser refresh
2. Admin group members can access referential management while regular users cannot
3. API validates EntraID tokens on every request and rejects invalid tokens
4. Dev mode allows local development without authentication when enabled
5. All referentials can be created, edited, and deleted with usage protection
6. Each referential item shows usage count and deletion is blocked when item is in use
</verification>

<success_criteria>
All 6 phase success criteria verified by user.
No blocking issues remain.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-authentication/01-07-SUMMARY.md`
</output>
