# Use the development database for tests
# Tests clean up after themselves using clearTestData()
# In production CI/CD, create a separate test database
TEST_DATABASE_URL=postgres://postgres:postgres@localhost:5432/eurostar_portfolio
DEV_MODE=true
